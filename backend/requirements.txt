# =============================================================================
# backend/requirements.txt — Python-залежності для FastAPI-бекенду
#
# Версії закріплені (pinned) для відтворюваних збірок.
# Оновлюйте регулярно командою: pip list --outdated
# =============================================================================

# --- Web Framework -----------------------------------------------------------
# FastAPI: сучасний, швидкий веб-фреймворк для побудови API
fastapi==0.115.5

# Uvicorn: ASGI-сервер з підтримкою HTTP/1.1 та WebSockets
# [standard] включає watchfiles (hot-reload), httptools, uvloop
uvicorn[standard]==0.32.1

# --- Database ----------------------------------------------------------------
# SQLAlchemy: ORM та SQL toolkit
# Версія 2.x підтримує async та новий декларативний стиль
sqlalchemy==2.0.36

# asyncpg: нативний async-драйвер PostgreSQL для Python (швидший за psycopg2)
# Використовується SQLAlchemy через схему `postgresql+asyncpg://`
asyncpg==0.30.0

# greenlet: необхідна залежність для SQLAlchemy asyncio extension
greenlet==3.1.1

# Alembic: інструмент для міграцій схеми бази даних
alembic==1.14.0

# --- Validation & Settings ---------------------------------------------------
# Pydantic: валідація даних та серіалізація (вбудований у FastAPI)
pydantic==2.10.1

# pydantic-settings: завантаження конфігурації з .env та змінних оточення
pydantic-settings==2.6.1

# email-validator: необхідний для типу EmailStr у Pydantic
# Встановлюється як extra: pydantic[email]
email-validator==2.2.0

# --- Authentication ----------------------------------------------------------
# python-jose: робота з JWT-токенами (підпис та перевірка)
python-jose[cryptography]==3.3.0

# passlib: хешування паролів (bcrypt)
passlib[bcrypt]==1.7.4

# --- HTTP & CORS -------------------------------------------------------------
# python-multipart: підтримка form data (необхідно для OAuth2PasswordRequestForm)
python-multipart==0.0.17

# --- Development & Testing ---------------------------------------------------
# pytest: фреймворк для тестування
pytest==8.3.3

# httpx: async HTTP-клієнт, необхідний для TestClient FastAPI
httpx==0.27.2
